apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nanobanana-cli
  name: nanobanana-cli
  namespace: acedatacloud
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: nanobanana-cli
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: nanobanana-cli
    spec:
      containers:
        - name: nanobanana-cli
          image: ghcr.io/acedatacloud/nanobanana-cli:${TAG}
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
      dnsPolicy: ClusterFirst
      restartPolicy: Always
